# 업데이트 내역

## 2026-09-26 — OpenAI Responses v1.7.10

- 전용 OCR 모델을 `gpt-6-ocr`로 변경했습니다. 첫 처리는 GPT-6 Luna/low, 출력 검사 실패 시 재시도는 GPT-6 Sol/medium을 사용합니다.
- 저장된 모델 목록의 `gpt-5.6-ocr`를 새 이름으로 이전하고 중복 항목을 정리합니다. 기존 대화/커스텀 모델의 옛 OCR ID도 GPT-6 경로로 연결됩니다. 일반 GPT-5.6 모델 및 자동 라우팅은 유지합니다.
- `OCR_TERRA_FALLBACK`을 `OCR_SOL_FALLBACK`으로 이전하며 기존 False 설정을 보존합니다. 두 설정이 있으면 새 설정이 우선합니다. 저장된 GPT-5.6 Terra/Sol OCR fallback 모델은 GPT-6 Sol로 이전합니다.
- PDF 원본 전송, 기본 10페이지 분할, 원래 사용자 프롬프트 복원, RAG/웹/도구 격리, 임시 업로드 삭제를 유지하고 안내 문구를 갱신했습니다.
- 검증: 오프라인 테스트 93개 통과. 신규 6개 테스트로 옛/새 OCR ID의 실제 Pipe 분기, 설정 이전·원본 불변, Luna 성공 시 재시도 없음, 불완전 응답의 Sol 재시도, 재시도 비활성화 유지, 모의 PDF 묶음의 페이지 범위·업로드 정리를 확인했습니다. 배포 JSON·Python 문법·diff 검사를 수행했습니다.
- 한계: HTTP/API 업로드와 PDF 분할 결과는 신규 테스트에서 모의 처리했습니다. 실제 OpenWebUI 서버 설치, 유료 API 호출, OCR 정확도·지연·모델 접근 권한은 검증하지 않았습니다.

## 2026-09-26 — Google Gemini v1.23.6

- 사용자가 제공한 Google Gemini v1.23.6 내보내기 코드로 공개본 v1.20.3을 업데이트했습니다. 기존 작성자 표시와 Apache License 2.0을 보존했으며, 실행 코드는 첨부본과 동일합니다. 개인 내보내기의 사용자 ID는 게시하지 않습니다.
- 일반 Gemini Flash 요청의 로컬 Auto Thinking 선택, 진행 상태·첫 응답 시간 표시, API가 반환한 thought summary 표시를 반영했습니다.
- HWPX ZIP/XML 본문 및 내장 이미지 처리, 파일/도구 처리 개선과 요청별 사용자 컨텍스트 분리를 반영했습니다.
- OCR 기본 모델을 첨부본의 Gemini 3.8 Flash로 반영했습니다. RAG 우회에서 PDF 원본 전달을 유지하고, OCR 원본 전달 실패 시 일부 추출 텍스트로 대체하지 않고 오류를 반환합니다.
- 최신 JSON과 v1.23.6 고정 JSON을 생성했습니다. 별도 RAG Bypass 필터와 OpenAI 함수는 변경하지 않았습니다.
- 검증: 원본 PDF 바이트 보존, 대용량 Files API 분기, OCR 원본 누락/Vertex 대용량 실패, HWPX 한글·섹션 순서·미리보기·길이 제한·압축 해제 크기 제한에 대한 오프라인 helper 테스트 7개 통과. Python 문법·배포 JSON 일치·첨부본 실행 코드 일치·비밀값 패턴 및 diff 검사를 수행했습니다.
- 한계: helper 테스트는 AST로 실제 메서드를 추출하고 외부 파일/API 경계를 모의 처리합니다. 전체 OpenWebUI/Google SDK 통합, 실 API 모델 접근, 스캔 OCR 정확도·속도·요금은 검증하지 않았습니다. 기본 Python에 Pydantic이 없어 기존 OpenAI 전체 회귀 테스트는 이번 작업에서 실행하지 않았습니다.

## 2026-09-26 — 처음 사용자를 위한 README 개편

- 사용자 화면에서 확인된 문서 도구 버전을 기록하고, 실제 서버의 미확인 포트/볼륨/메모리 설정과 신규 설치 예시를 구분했습니다.
- Windows PowerShell 기준 Dockerfile·Compose·Terminal 암호 준비, 관리자 연결 주소, 함수 설정, 첨부/문서 생성 테스트를 단계별로 설명했습니다.
- 기존 사용자는 재설치하지 않도록 안내하고 개인 지침/공통 도구/사용자 파일의 차이와 백업·문제 확인 방법을 추가했습니다. 하네스는 선택 항목으로 이동했습니다.
- 검증: README의 Compose 예시 파싱과 필수 설정, 로컬 링크, Markdown 코드 블록, 배포 파일 일치 검사를 수행했습니다. 실제 Windows/Docker 신규 빌드는 실행하지 않았습니다. 함수 코드와 버전은 변경하지 않았습니다.

## 2026-09-26 — OpenAI Responses v1.7.9

- 기본값을 간단 모드(`DOCUMENT_WORKFLOW_MODE=simple`)로 변경했습니다. 기존 Terminal에 연결하고 양식을 첨부하여 작업을 요청하면 됩니다. 하네스 ZIP·볼륨·catalog·스킬 설치를 요구하지 않습니다.
- 기존 kordoc·PPT 라이브러리와 적절한 기존 개인 지침을 활용하는 짧은 공통 제작 안내를 제공합니다. getdesign 참조는 요청한 경우에만 준비하고, 없는 학교 양식·의존성·글꼴은 구체적으로 알리도록 안내합니다.
- 하네스는 명시적 `harness` 모드에서만 등록합니다. 이전 ENABLE_TERMINAL_HARNESS=True가 남아 있어도 간단 모드가 우선합니다. 기존 파일은 삭제/이전하지 않습니다.
- 대기 문구 정리·첨부 전달·파일 카드를 유지합니다. 모델/Terminal 권한이 있는 사용자가 같은 안내를 사용할 수 있으며 개인 지침이나 파일을 자동 공유하지 않습니다.
- 검증: 오프라인 테스트 80개 통과. 기존 회귀와 기본 모드/고급 모드/미연결/백그라운드 작업 분기, 실제 Pipe 요청 지침 주입을 검사했습니다. 배포 JSON·Python 문법·변경 파일 검증을 수행했습니다.
- 한계: 실제 사용자 서버/API·문서 렌더링 품질·getdesign 네트워크 다운로드는 이번 변경에서 검증하지 않았습니다. 간단 모드는 실행 엔진 추가가 아닌 기존 도구를 사용하는 모델 지침이며, 필요한 기존 라이브러리는 설치돼 있어야 합니다.

## 2026-09-26 — OpenAI Responses v1.7.8 / 공통 하네스 1.1.0

- 특정 PPT 기능 연결을 공통 AGENTS.md·core/site catalog 기반으로 확장했습니다. core는 버전별 공통 자산, site는 학교 지침·양식, workspace는 실제 실행 계정별 작업 폴더입니다. 새 스킬은 목록 등록으로 확장합니다.
- ENABLE_TERMINAL_HARNESS, TERMINAL_HARNESS_ENTRYPOINT, TERMINAL_HARNESS_SITE_ROOT를 추가했습니다. 기존 ENABLE_PRESENTATION_DESIGN의 꺼짐 설정도 보존하여 이전하고 명시적 새 설정을 우선합니다. 기본 진입점은 AGENTS.md, INDEX.md는 호환 안내로 유지합니다.
- 학교 스킬의 명시적 override/비활성화, 중복·누락·외부 경로 검증, 비파괴 site 초기화 및 실제 OS 계정 기준 작업 폴더 생성 스크립트를 추가했습니다. 사용자 ID/HOME 입력으로 타인 홈을 선택하지 않습니다.
- kordoc/HWPX·가정통신문·규정 신구대조표의 새 기본 지침을 추가했습니다. 기존 개인 스킬의 실제 내용은 아직 확보하지 못했으므로 동일 구현이라고 주장하지 않습니다. 원본 보존·관리자 검토·site 등록 이전 절차와 전체 사용자 접근 권한/두 계정 확인 절차를 제공합니다.
- 실제 학교 양식·개인정보·비밀값은 배포에 포함하지 않습니다. 기존 1.0.0 ZIP 및 과거 함수 JSON은 유지합니다. 공통 하네스는 사용 권한을 부여하거나 사용자 간 보안 격리를 구현하지 않습니다.
- 검증: 오프라인 테스트 77개, core/site 목록의 명시적 대체·비활성화·확장, 누락 양식·외부 심볼릭 링크 거부, 기존 site 보존, 모의 두 계정의 작업 경로 분리, 설정 이전 및 기존 PPT/진행 표시/첨부 회귀 검사. 스킬 구조·배포 JSON·ZIP/SHA-256을 검증했습니다.
- 한계: 실제 OpenWebUI 일반 사용자 계정/서버 설치, 기존 kordoc 지침 원문 이전, 실제 학교 HWPX 양식 출력 및 getdesign 네트워크 다운로드는 미검증입니다. 서버 배포는 설치 안내에 따라 별도로 수행해야 합니다.

## 2026-09-25 — OpenAI Responses v1.7.7 / Terminal 하네스 1.0.0

- 함수의 상세 PPT 제작 지침과 생성용 다운로드 명령을 제거하고, Terminal의 INDEX.md를 읽도록 안내하는 `get_terminal_harness`로 교체했습니다. 하네스 ZIP·SHA-256·Windows/WSL2 Compose 마운트 안내를 제공합니다.
- 제작/디자인 참조/테마 변환 지침을 필요할 때만 읽습니다. 환경 진단, 격리 다운로드, 기본 편집형 PPTX 빌더, 구조·슬라이드 경계 검증 및 예제를 패키지에 포함했습니다. 별도 스킬 자동 등록이나 서버 자동 설치는 하지 않습니다.
- `TERMINAL_HARNESS_ROOT=/opt/openwebui-harness` 및 `COMPACT_STATUS_UPDATES=True`를 추가했습니다. 활성화는 기존 `ENABLE_PRESENTATION_DESIGN`을 유지합니다.
- 같은 API 요청의 response.created / response.in_progress가 동일 대기 문구를 두 번 출력하던 문제를 수정했습니다. 간결 모드는 도구 후속 요청에서도 대기를 반복하지 않고 긴 인자/결과 상태 표시를 줄입니다. 45초 무활동 알림은 같은 대기 구간에서 한 번만 표시합니다. 오류·파일 카드·실제 도구 결과는 유지하며 기존 저장 기록은 변경하지 않습니다.
- 검증: 오프라인 테스트 70개 통과. 한글 텍스트가 있는 편집형 PPTX를 실제 로컬 생성하고 텍스트/슬라이드 수/경계 및 덮어쓰기 방지를 확인했습니다. 다운로드 실패·HTML·빈 결과·시간 초과, locator 등록/비활성화/충돌, lifecycle 중복·도구 후속 요청·상태 간결화를 검사했습니다. 배포 JSON·ZIP·SHA-256 일치 및 스킬 구조 검증을 수행했습니다.
- 한계: getdesign 실네트워크 호출, 실제 OpenWebUI·WSL2 서버 설치, 슬라이드 렌더링/시각 품질은 검증하지 않았습니다. 디자인 해석과 Terminal 실행은 모델의 지침 준수에 의존합니다. PPTX에 글꼴은 자동 포함되지 않습니다.

## 2026-09-25 — OpenAI Responses v1.7.6

- getdesign.md 또는 첨부 DESIGN.md를 활용한 PPT 제작용 `prepare_presentation_design` 도구와 지침을 추가했습니다. Terminal 연결 및 `ENABLE_PRESENTATION_DESIGN=True`에서만 등록되며 백그라운드 제목 생성에는 등록하지 않습니다.
- 사용자 지정 스타일의 참조 다운로드 → PPT 토큰 정리 → 제작 → 검증 → display_file 반환 절차를 모델에 제공합니다. 다운로드와 PPT 제작은 기존 Terminal 도구를 통해 실행되며 새 도구 자체는 실행 계획만 반환합니다.
- CLI 인자 검증, 고유 임시 디렉터리, 120초 다운로드 제한, 빈 파일·HTML·실패 결과 검사를 포함했습니다. 외부 Markdown은 디자인 데이터로만 취급하도록 안내합니다.
- 한글 글꼴 확인, 편집 가능한 슬라이드, 렌더링 가능 여부 보고 및 다운로드 카드 반환을 절차에 포함했습니다. 기존 라우팅·첨부 전송·파일 카드 처리는 유지합니다.
- 검증: 오프라인 테스트 62개(기존 57개 + 신규 5개), 배포 JSON 일치 및 diff 검사. 다운로드 성공/실패/빈 파일/HTML은 모의 실행으로 검사했습니다.
- 한계: 실제 getdesign CLI 다운로드, 유료 카탈로그 접근, OpenWebUI 서버/API 및 PPT 시각 품질은 검증하지 않았습니다. 실제 작업은 모델의 도구 선택과 Terminal 설치 환경에 의존합니다. 서버 자동 설치는 포함하지 않습니다.

## 2026-09-25 — OpenAI Responses v1.7.5

`Chat Uploads = Default`를 유지하면서, 원본 양식·표·수식이 필요한 작업에서만 선택한 첨부 원본을 Open Terminal로 가져오는 기능을 추가했습니다. Docker 서버를 추가할 필요가 없습니다.

- `list_chat_attachments`: 현재 첨부와 현재 대화 분기의 이전 사용자 첨부를 조회합니다. 목록 조회만으로 파일을 전송하지 않습니다.
- `prepare_terminal_files`: 허용된 첨부 ID만 관리자 Terminal 프록시를 통해 전송합니다. 사용자 접근 권한을 다시 확인하며 임의의 로컬 경로·URL을 받지 않습니다.
- 원본 복사본은 Terminal 사용자 홈의 `.openwebui-attachments` 아래 사용자·대화별 경로에 저장합니다. 전송 및 재사용 시 실제 파일 바이트의 크기와 SHA-256을 검증합니다. 같은 이름의 파일도 첨부 ID와 내용으로 구분합니다.
- 일반 읽기·요약에서는 전송하지 않도록 모델 지침을 추가했습니다. 실제 전송 여부는 모델의 도구 선택에 기반하므로 사용자 의도를 100% 판별하는 규칙 기반 분류기는 아닙니다.
- 새 Valve: `ENABLE_TERMINAL_ATTACHMENT_TRANSFER=True`, `TERMINAL_ATTACHMENT_MAX_MB=50`. 한 번에 최대 5개를 선택할 수 있습니다. 저장되는 대화에서 관리자 Terminal 연결을 선택하고 업로드 설정은 Default로 유지하세요. 개인 Direct 연결의 자동 첨부 전달은 지원하지 않습니다.
- 기존 PDF/HWPX 분석, 전용 OCR·이미지 처리, 모델 라우팅 및 v1.7.4 파일 카드 처리를 유지합니다. 현재 첨부가 기본 분석 경로에서 처리되는 동작 자체를 중단하지는 않습니다.
- 원본 복사본은 자동 삭제하지 않습니다. 폴더 구분은 대화별 실행 샌드박스가 아닙니다. 기존 원본이 삭제됐다면 같은 대화에서도 다시 가져올 수 없습니다.
- 검증: 저장소 오프라인 테스트 57개 통과(기존 29개 + 첨부 전송 21개 + 등록·보존 7개). 별도로 공식 OpenWebUI helper를 사용한 기존 파일 카드 bridge 10개 시나리오, Python 문법·JSON 배포 일치 검증 통과.
- OpenWebUI 0.11.4와 작업 시점 Open Terminal 공식 소스로 API 경로를 확인했습니다. 실제 Windows/Docker 서버 설치, 실제 API 응답 및 문서 생성 품질은 검증하지 않았습니다. 설치 후 요약 → 같은 첨부를 이용한 양식 생성 → 재사용 → 카드 다운로드 순으로 확인해야 합니다.

## 2026-09-25 — OpenAI Responses v1.7.4

사용자가 제공한 v1.7.4 내보내기 파일의 Python 코드를 그대로 반영했습니다. 기존 공개 원본 v1.6.8 이후의 변경을 포함하며, 최신 JSON과 v1.7.4 고정 배포 파일을 함께 제공합니다.

- **1.7.4:** OpenWebUI 화면에 전달하는 함수 실행 결과를 `input_text` 배열로 표현합니다. OpenAI에 전달하는 결과 문자열은 유지합니다.
- **1.7.3:** Terminal 응답의 Mapping 형식 헤더를 일반 dict로 정규화하여 파일 카드와 갱신 이벤트 처리에 사용합니다.
- **1.7.1–1.7.2:** Terminal 파일 표시·다운로드 연결을 추가하고, 최종 응답에도 파일 출력을 보존합니다. 비동기 도구 목록을 요청 제한 시간 안에서 처리합니다.
- **1.7.0:** 코드의 GPT-6 자동 라우팅을 Luna/Sol/Astra로 구성하고 Sol을 기본으로 사용합니다. 이전 라우팅 설정을 이전하며 요청 파라미터 정리를 공통화합니다.
- 기존 공개 코드 대비 함수 호출 반복 상한의 기본값이 10에서 64로 변경되었습니다. 복잡한 도구 작업은 호출 횟수와 비용이 늘어날 수 있습니다.
- 검증: 기존 오프라인 회귀 테스트 27개와 Terminal 표시·실패 처리 테스트 2개 통과. Python 문법, JSON 구조, 버전 정보 및 첨부 원본과 배포 코드의 일치를 확인했습니다.
- 실제 OpenWebUI 서버, 실제 API 및 Terminal 서비스 연결은 검증하지 않았습니다. 서버 자동 설치는 수행하지 않았습니다.

## 2026-09-25 — Codex 연동 설정

- 공개 중인 함수의 Python 편집 원본을 추가했습니다: OpenAI Responses 1.6.8, Google Gemini 1.20.3, Gemini RAG Bypass 1.0.1.
- 함수별 최신 JSON 및 버전별 JSON을 만드는 도구를 추가했습니다.
- Codex 작업 완료 시 검증, 변경 내역 기록, 커밋, GitHub 업로드를 수행하는 규칙을 추가했습니다.
- 기존 JSON과 함수 동작은 변경하지 않았습니다. 로컬 작업물인 Responses 1.7.0은 포함하지 않았습니다.
- 검증: Python 원본 및 기존 JSON 내 코드의 문법 확인, 원본과 같은 버전의 JSON 코드 일치 확인, 임시 복제본에서 새 버전 생성·메타데이터 반영·기존 버전 덮어쓰기 거부·불일치 검출 확인. 실제 OpenWebUI 서버 동작은 이번 설정의 검증 범위에 포함되지 않습니다.
